# Environment variables (secrets) - matches ALL directories
.env
.env.local
.env.*.local
**/.env
**/.env.local
**/.env.*.local

# Rust
target/
Cargo.lock  # For binaries (keep for libraries)
**/*.rs.bk
*.pdb

# Node.js / PNPM
node_modules/
.pnpm-store/
dist/
.turbo/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build artifacts
*.so
*.dylib
*.dll
*.a
*.lib
*.exe
*.out
*.app

# IDE / Editor
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
*.sublime-project
*.sublime-workspace

# Testing
coverage/
*.profraw
*.profdata

# Documentation builds
packages/docs/.astro/
packages/docs/dist/

# OS files
.DS_Store
Thumbs.db
Desktop.ini

# Temporary files
tmp/
*.tmp
*.temp
.cache/

# Old page folders (consolidated into admin-pages/)
signup-page/
privacy-page/
terms-page/

# Build artifacts (Cloudflare Wrangler)
.wrangler/
.wrangler-test/
wrangler-output/

# User configuration
config.toml  # User-specific config
!planning/**/*.toml  # Keep example configs in planning/

# Certificates (generated locally)
*.pem
*.key
*.crt
!planning/**/*.pem  # Keep test certs in planning/

# Database files
*.db
*.db-shm
*.db-wal

# Audio recordings (user data)
recordings/
*.wav
*.mp3
*.m4a
*.flac
*.ogg
*.aac
!tests/fixtures/**/*.wav  # Keep test audio

# Voice training samples (copyrighted, keep local only)
packages/personas/*/audio/samples/*
!packages/personas/*/audio/samples/.gitkeep

# Logs
logs/
*.log

# Backup files
*.bak
*.backup
scripts/bin/

# Python virtual environments (don't commit venvs)
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# HuggingFace model cache (models downloaded at runtime)
.cache/huggingface/
~/.cache/huggingface/

# Git LFS (local cache - objects stored in R2)
.git/lfs/tmp/

# Test files (exclude from repo unless in tests/ directory)
test-*.js
!tests/**/*.js

# Documentation (temporary files)
*.md.bak
*.md.tmp

# Development environment files
.dev.vars
.dev.env

# Claude Code artifacts
.claude/agents/
.claude/settings.local.json
.claude/context/

# Python TUI testing - temporary dev tests (NOT committed)
tmp/dev_tests/
tmp/dev_tests/__snapshots__/

# Keep permanent test snapshots (COMMITTED)
!packages/assistant/tests/__snapshots__/

# HTML test/snapshot reports (may contain env var data)
snapshot_report.html
**/*snapshot_report*.html

# Configuration (generated from config.js to ./tmp/)
# config.js is the only config file that should be committed
# All generated config files go in ./tmp/ directory
