{
  "name": "@xswarm/mcp-server",
  "version": "0.1.0",
  "description": "xSwarm MCP isolation server (Rust binary)",
  "scripts": {
    "build": "cargo build",
    "dev": "cargo run",
    "test": "cargo test",
    "clean": "cargo clean"
  },
  "keywords": ["rust", "mcp", "security"],
  "license": "MIT"
}
